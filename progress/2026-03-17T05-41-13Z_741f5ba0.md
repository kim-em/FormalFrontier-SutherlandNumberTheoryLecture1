## Accomplished

- Polished `Example1_24.lean`:
  - Replaced non-terminal `simp [a, b]` with `simp only [AddSubgroupClass.coe_sub, SubmonoidClass.coe_pow, MulMemClass.coe_mul, ZeroMemClass.coe_zero]` (fixes linter warning)
  - Cleaned up `from by` → `by` syntax in irrational proof
- Polished `Example1_29.lean`:
  - Inlined repeated `h7`/`hsq` sqrt 7 setup into single `nlinarith [Real.sq_sqrt ...]` calls (removed 4 lines of boilerplate)
  - Fixed shadowed `this` bindings by giving explicit names `h_eq` and `h_two_n`
- Both files build cleanly with zero warnings

## Current frontier

Stage 3.4 proof polishing for Example1_24 and Example1_29 is complete.

## Overall project progress

- 27/27 items sorry-free
- Stage 3.4 (Proof Polishing): Theorem1_9 and Lemma1_4 done (#117 merged), Example1_24 and Example1_29 done (this PR)
- Remaining Stage 3.4 work: lint pass (#116), completion gate (#118)

## Next step

- Merge this PR, then work on #116 (lint pass for Proposition1_28 + full project audit)
- After #116, close out Stage 3.4 via #118

## Blockers

None.
